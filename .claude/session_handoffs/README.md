# Draft session handoffs

Thư mục này chỉ chứa handoff được tạo khi bộ chỉ dẫn trong `.claude/` được dùng trong một phiên có thay đổi repo hoặc trạng thái vận hành. Không tạo handoff cho phiên chỉ đọc, khảo sát hoặc hỏi đáp.
