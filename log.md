# Log

> Nhật ký append-only: mỗi dòng ghi nhận 1 operation (ingest / tạo trang từ query / lint). Định dạng: `[YYYY-MM-DD] <operation> <mô tả ngắn>`.

[2026-09-12] ingest `imf_macro_accounting` Chương 2, cụm A (đại lượng hạch toán quốc gia) → tạo 14 trang: system-of-national-accounts-sna, value-added, gdp, production-income-and-expenditure-approaches-yield-the-same-gdp, real-gdp, gdp-deflator, measured-gdp-covers-only-what-markets-price, gni, gndi, absorption, gross-national-saving, current-account-deficit-means-absorption-exceeds-national-income, the-saving-investment-gap-equals-the-current-account-balance, the-private-sector-resource-gap-must-be-financed-by-other-sectors. Tạo 6 stub: current-account-balance, depreciation, final-consumption, gross-investment, inflation, mps-material-product-system. Chương 2 còn 6 cụm chưa ingest (B: đo lường kinh tế chuyển đổi, C: lạm phát, D: thu nhập & lao động, E: chính sách giá & thu nhập, F: case Poland, G: khung kế toán SNA chi tiết).

[2026-09-12] ingest `imf_macro_accounting` Chương 2, cụm B (đo lường kinh tế chuyển đổi) + cụm C (lạm phát) → nâng 2 stub thành draft (mps-material-product-system, inflation), tạo 8 trang mới: transition-statistics-understate-private-sector-growth, a-real-output-index-needs-prices-that-reflect-relative-scarcity, to-what-extent-was-polands-output-decline-1990-91-overstated, core-inflation-strips-out-one-time-price-level-jumps, a-one-time-price-shock-becomes-inflation-only-if-monetary-policy-accommodates-it, types-of-inflation, inertial-inflation-persists-because-it-is-written-into-contracts, laspeyres-and-paasche-indexes-bracket-true-inflation.

[2026-09-12] ingest `imf_macro_accounting` Chương 2, cụm D (thu nhập & lao động) + cụm E (chính sách giá & thu nhập) → tạo 10 trang mới: real-wages, real-wage-growth-is-bounded-by-productivity-growth, unemployment-rate, nairu, types-of-unemployment, price-liberalization, price-convergence-keeps-pressure-on-inflation-through-the-transition, incomes-policy, designing-wage-controls-means-choosing-a-norm-indexation-coverage-and-enforcement, wage-controls-lose-effectiveness-rapidly-after-a-short-period.

[2026-09-12] ingest `imf_macro_accounting` Chương 2, cụm F (case Ba Lan) + cụm G (khung kế toán SNA chi tiết) → tạo 12 trang mới (6 case Ba Lan + 6 trang khung SNA); cập nhật system-of-national-accounts-sna thành trang hub trỏ tới các trang cụm G để 3 trang này có backlink. Chương 2 đã ingest xong toàn bộ 7 cụm. Còn 4 stub chờ các chương sau: current-account-balance (Chương 4), depreciation, final-consumption, gross-investment.

[2026-09-12] bồi đắp `imf_macro_accounting` Chương 2 → gỡ toàn bộ 4 stub thành draft bằng vật liệu có sẵn trong Chương 2 (final-consumption, gross-investment, depreciation từ mục Main Aggregates; current-account-balance từ chú thích 10) — lượt ingest cụm A ghi nhầm rằng 3 stub đầu phải chờ chương sau. Tạo 1 trang mới lấp mảng còn thiếu: leakages-equal-injections-in-the-circular-flow (mục The Circular Flow of Income and Spending chưa từng được trích). Vá backlink cho 9 trang chỉ có 1 đường vào: gdp→residency, value-added→valuation, real-gdp→laspeyres, types-of-unemployment→nairu, sna-sequence-of-accounts→accrual + 1993-vs-1968, polish-price-liberalization→price-liberalization (và chiều ngược), incomes-policy→wage-controls-lose-effectiveness, gross-national-saving→polish-national-saving, production-income-approaches + private-sector-resource-gap→leakages. Wiki còn 51 trang + index, 0 stub, 0 link chết, 0 trang mồ côi, mọi trang có ≥1 outlink và ≥1 backlink.

[2026-09-14] schema-batch xử lý 7 issue audit trong 1 lượt sửa gộp (theo ghi chú vận hành CLAUDE.md: không sửa vụn vặt từng lần để giữ prompt cache). (1) Thêm mục `## Sources` đầu `02_wiki/index.md` — bảng trạng thái 2 nguồn, dữ liệu điền từ 5 dòng log hiện có; bắt buộc cập nhật mỗi lượt ingest qua `agents.md` ràng buộc 6 + `ingest/SKILL.md` bước 6 (kể cả lượt không tạo trang mới). (2) Thêm `00_schema.md` §10 định nghĩa sidecar `01_sources/<nguồn>/_ingest_state.md` — bản đồ chunk checkbox `[x]`/`[~]`/`[ ]`, bắt buộc kèm dải dòng — làm nguồn sự thật duy nhất cho câu hỏi "còn lại phần nào", thay cho việc diễn giải lại văn xuôi `log.md`; tạo file này cho `imf_macro_accounting` với 13 chunk (Ch.2 xong toàn bộ 7 cụm A–G; còn Ch.1, Ch.3, Ch.4, Ch.5, Ch.6; front matter đánh xong-bỏ-qua). Đi kèm: khoét đúng 1 ngoại lệ cho luật cứng bất biến `01_sources/` (chỉ file tiền tố `_` do agent sở hữu, không tạo thêm file nào khác) — ghi ở `00_schema.md` §3, `CLAUDE.md` luật 1, `agents.md` ràng buộc 1. Ranh giới chương lấy từ heading nguồn; ranh giới cụm A–G là DỰNG LẠI từ heading chứ không phải số liệu gốc (log cũ đặt tên cụm nhưng không ghi dải dòng) — cảnh báo này ghi ngay trong state file. (3) Thêm `00_schema.md` §7 luật 5: claim lấy từ nguồn dài kèm chú thích vị trí `(<nguồn>, <chương>, <mục>, d.<từ>–<đến>)` ngay sau claim, không dồn cuối trang; cập nhật ví dụ `gdp` trong §7 với 4 chú thích trỏ 4 mục khác nhau của Ch.2. NỢ KỸ THUẬT: 51 trang hiện có chưa có chú thích vị trí — cố ý không backfill hàng loạt ở lượt này, để lại thành khoản nợ riêng. (4) Ngưỡng nguồn dài đặt ở `00_schema.md` §10 chứ không ở `ingest/SKILL.md` như gợi ý — lý do: ngưỡng quyết định nguồn có state file hay không nên nó là thuộc tính data model, skill chỉ tiêu thụ; ngưỡng = >120 KB HOẶC >1.200 dòng HOẶC một chương đơn lẻ tự vượt hai mức đó (~120 KB ≈ 30k token, quá đó thì không còn chỗ context cho bước đối chiếu toàn bộ `02_wiki/`). Cố ý không dùng số heading làm tiêu chí: `Modern Money Mechanics` có 66 heading trong 721 dòng vẫn đọc trọn 1 lượt. Đo thực tế: `imf_macro_accounting` 829 KB/6.065 dòng = nguồn dài; `Modern Money Mechanics` 83 KB/721 dòng = nguồn ngắn, chưa ingest. (5) Chọn THÊM `_inbox.md` ở gốc dự án (không bỏ qua) + §11 quy ước triage mỗi lượt lint với đúng 3 kết cục bắt buộc; lý do thêm dù wiki mới 51 trang: `status: stub` chỉ giữ chỗ cho khái niệm ĐÃ BIẾT TÊN, không chứa được câu hỏi chưa có tên, nên stub không thay thế được vùng capture. (6) Thêm tiêu chí lint *Nhiễu OCR còn sót* với 6 họ mẫu (l/O thay chữ số, số chú thích dính vào từ, chữ số bị tách, HOA–thường lẫn trong từ, nguyên âm nhân đôi lạ, rác markdown OCR); bảng lint 8 → 10 tiêu chí (thêm cả *Nợ inbox*). CHẠY THỬ TRÊN 51 TRANG: 2 trang có hit (`gdp-deflator`, `laspeyres-and-paasche-indexes-bracket-true-inflation`), cả hai đều là BÁO GIẢ — token `Paasche` là tên người có thật; 0 lỗi OCR thật lọt vào wiki. Bộ mẫu đã đối chứng ngược trên lỗi thật của nguồn IMF (`Waaes`, `lmbalance12`, `Economiesl3`, `DeficitlO`, `tO`, `1 993`, `COLmtries`): bắt 8/9 mẫu thử. Giới hạn đã biết và đã ghi vào skill: không bắt được lỗi OCR tạo ra TỪ CÓ THẬT (`set our` thay `set out`, `Governmenr` thay `Government`). (7) Khác gợi ý: không chọn nhị phân "thêm field" / "chấp nhận đánh đổi" mà chọn đường giữa — thêm trường `reviewed:` TUỲ CHỌN (§1, §9), hook không kiểm, chỉ người dùng được đặt, 51 trang cũ không phải backfill; cố ý KHÔNG thêm tiêu chí lint cho trường này vì ở quy mô một người dùng nó chỉ sinh nhiễu phải bỏ qua mỗi lượt — lý do ghi thẳng vào §9 để lần audit sau không tính là thiếu sót chưa xem xét. Tham chiếu schema cập nhật kèm: ingest §1,§2,§4–§11 và lint §4–§9,§11 ở cả `CLAUDE.md` lẫn `agents.md`. LƯU Ý: đề bài nêu 8 issue nhưng chỉ liệt kê 7 — issue thứ 8 chưa nhận được, chưa xử lý.

[2026-09-14] đính chính batch cùng ngày — hoàn nguyên ngoại lệ đã khoét vào luật cứng 1 (`01_sources/` bất biến). Lượt trước đặt file trạng thái ingest tại `01_sources/imf_macro_accounting/_ingest_state.md` và sửa luật cứng 1 ở `CLAUDE.md`, ràng buộc 1 ở `agents.md`, §3 ở `00_schema.md` để hợp thức hoá bằng lập luận "file tiền tố `_` không phải tài liệu nguồn". Lập luận đó SAI ngay trên chính repo này: `01_sources/Modern Money Mechanics/` đã có sẵn 15 file `_page_*.jpeg` là vật liệu nguồn thật do OCR sinh ra, nên tiền tố `_` không phân biệt được nguồn với không-nguồn; và một luật cứng đã có ngoại lệ thứ nhất thì sẽ có ngoại lệ thứ hai. Sửa: (a) tạo lớp thứ tư `03_state/` do agent sở hữu, chuyển file sang `03_state/imf_macro_accounting.md`, thêm trường `file:` khai đường dẫn nguồn đầy đủ vì file trạng thái không còn nằm cạnh nguồn; (b) xoá bản cũ trong `01_sources/` — thư mục nguồn nay chỉ còn đúng các file gốc (2 file IMF + 17 file Modern Money Mechanics), kiểm được bằng một câu hỏi duy nhất "có file nào trong `01_sources/` không phải tài liệu nguồn không?"; (c) khôi phục và SIẾT luật cứng 1 ở `CLAUDE.md` + ràng buộc 1 và ghi chú vận hành ở `agents.md` + §3 ở `00_schema.md` thành "không sửa, không thêm file, không đổi tên, không xoá — không ngoại lệ"; (d) `00_schema.md` §10 ghi rõ lý do chọn `03_state/` thay vì đặt cạnh nguồn, kèm dòng chốt để lần audit sau không đề xuất chuyển ngược lại; (e) thêm `03_state/` vào bảng 4 lớp kiến trúc ở `CLAUDE.md`; (f) cập nhật đường dẫn ở `02_wiki/index.md` §Sources, `ingest/SKILL.md` (bước 1, 3, 7 + 1 sai lầm mới: ghi bất cứ file nào vào `01_sources/` là vi phạm luật cứng 1), `query/SKILL.md`. Số dòng `d.<từ>–<đến>` dùng làm khoá tra cứu được chính vì `01_sources/` bất biến — ghi bổ sung nhận xét này vào §10, nó là lý do kỹ thuật khiến luật bất biến đáng giữ tuyệt đối chứ không chỉ là nguyên tắc. Không đụng tới `_inbox.md` (giữ ở gốc dự án, văn xuôi cho người đọc, khác bản chất với `03_state/` máy-đọc-được) và không đụng tới quyết định `reviewed:` tuỳ chọn ở issue 7 — đã rà lại, quyết định đó không liên quan gì tới `01_sources/`.

[2026-09-14] lint 51 trang (audit toàn dự án đối chiếu Karpathy + Evergreen Notes, theo yêu cầu người dùng — không phải lượt định kỳ §4) → 4 lỗi, 3 khoảng trống cơ chế, 3 mục theo dõi; ĐÃ BÁO CÁO, KHÔNG TỰ SỬA (luật cứng 3). LỖI: (L1) `lint/SKILL.md` kê sai lệnh grep cho tiêu chí backlink — `grep "[[<tên-trang>]]"` bị shell/BRE hiểu `[[...]]` là bracket expression nên vừa âm tính giả vừa dương tính giả: `types-of-inflation` ra 0 file (thực tế 3 trang trỏ tới), `real-wages` ra 52/52 file, `inflation` ra 40 file; tiêu chí trang-mồ-côi vì thế trả kết quả rác. Bản vá đã thử: `grep -rlE "\[\[<tên>(\||\])" 02_wiki/` và trừ `index.md` ra khỏi kết quả. Lỗi này do chính lượt schema-batch cùng ngày viết ra mà không chạy thử. (L2) Dạng `[[trang|alias]]` được dùng thật — 9 link ở 8 trang — nhưng không được định nghĩa ở §6 hay §7, và hook chỉ dung thứ nó do không bao giờ phân giải đích link; mọi công cụ phân giải link ngây thơ đều báo 8 link chết giả (lượt quét đầu của audit này đã dính đúng bẫy đó). (L3) 0/51 trang có chú thích vị trí nguồn §7.5 — nợ kỹ thuật đã ghi nhận, nay đo được. (L4) 16/37 tag chỉ dùng đúng 1 lần, tag 1 thành viên không lọc được gì, mâu thuẫn chính vai trò "chỉ mục lọc rẻ" ở §6. KHOẢNG TRỐNG CƠ CHẾ: (K1) operation Query chưa từng chạy — 0 trang `type: analysis`, 0 dòng query trong log; đây đúng là cơ chế compounding phân biệt thiết kế Karpathy với RAG, và cũng là bậc trên cùng "abstract note" của thang Matuschak, cả hai đang rỗng — wiki hiện là sản phẩm ingest tốt, chưa là sản phẩm tư duy. (K2) operation Lint chưa từng chạy trước lượt này — §9 buộc `draft → stable` phải qua 1 lượt lint sạch nên toàn bộ 51 trang kẹt ở `draft`; §4 hẹn lint mỗi 10 ingest mà mới có 5, nghĩa là không trang nào lên `stable` được trong 5 lượt ingest nữa. (K3) Dự án không phải git repo — gist Karpathy nêu Git làm cơ chế version history; lớp `02_wiki/` do agent sở hữu và ghi đè tại chỗ mỗi lượt ingest mà không có cách xem diff hay hoàn tác merge hỏng. THEO DÕI: (T1) `mps-material-product-system` 306 từ gộp ranh giới vật chất/phi vật chất với công thức quy đổi NMP→GDP, công thức quy đổi có thể tách trang; (T2) trang hub `system-of-national-accounts-sna` 361 từ nhắc lại cơ chế balancing item vốn thuộc `sna-sequence-of-accounts` — hợp lệ với API note của Matuschak nhưng cần canh để hub không nuốt trang con; (T3) 26/51 trang có đúng 2 backlink, đúng mức sàn, ngay sau dòng log 2026-09-12 ghi "vá backlink cho 9 trang" — mẫu phân bố giống thoả-mãn-chỉ-tiêu, dù 14 ngữ cảnh link lấy mẫu đều có lý do liên kết thật nên chưa kết luận vi phạm. ĐẠT SẠCH: 3 lớp toàn vẹn sau lần đính chính (`01_sources/` chỉ còn 19 file gốc); 0 link chết, 0 trang mồ côi, backlink thấp nhất = 2, trung bình 3,14 outlink/trang; index ↔ thư mục khớp 51/51; 0 heading trong thân bài, 0 danh sách "xem thêm", 0 thiếu field, 51/51 title khớp tên file; 0 câu tiếng Anh chép nguyên văn (§7.4 và nguyên tắc personal writing vững); quy ước title đúng cả 51 trang (42 concept + 2 entity dạng danh từ, 7 case dạng câu khẳng định, 1 dạng câu hỏi đúng chỗ vì nguồn chưa kết luận); độ dài thân bài 153–361 từ, trung vị 236 — dải hẹp, khớp chuẩn "viết trọn trong 1 lượt"; 0 lỗi OCR thật; bridge note có mặt thật (leakages, saving-investment gap, private-sector resource gap, real-wage bound); căng thẳng index.md phân cấp (Karpathy) vs ontology liên tưởng (Matuschak) đã giải đúng — thư mục phẳng, liên kết mang cấu trúc, index chỉ là điều hướng. Tiến độ nguồn: 1.403/6.065 dòng = 23% (mới Ch.2), còn 71% ở Ch.1/3/4/5/6.
